name: wickra-xray CodeQL configuration

# What CodeQL should not spend a pass on, and why.
#
# Without this every generated or vendored file under a binding is analysed as
# if it were hand-written source, and each new binding class arrives as a fresh
# batch of findings about code nobody wrote.
paths-ignore:
  # napi-derive's expansion, not this file. Every exported `#[napi]` class
  # raises one `rust/access-invalid-pointer` anchored on its `pub struct` line,
  # while the file itself contains no `unsafe` at all -- the dereference the
  # rule sees lives in the macro, and CodeQL attributes it back to the macro's
  # source span. The count grows by one per class added and the rule can say
  # nothing true about the file, so nothing is lost by excluding it.
  #
  # By path rather than by disabling the query: `query-filters` match on id,
  # tags and severity and cannot be scoped, so switching the rule off would
  # also switch it off for `bindings/c/src`, which is where the real raw-pointer
  # code lives and where it earns its place.
  - bindings/node/src/lib.rs
  # Generated by `napi build` and committed so the package can be published
  # without a build step. Regenerating them is the fix for anything found here.
  - bindings/node/index.js
  - bindings/node/index.d.ts
  # A vendored copy of the C ABI header, kept in sync by
  # .github/scripts/check-cbindgen.sh rather than edited.
  - bindings/go/include
  # Fixtures, not code: the committed dataset, specs and expected frames.
  - golden
  # Build output and dependency trees.
  - target
  - "**/node_modules"

# The examples are deliberately small and skip the error handling a library
# would need, so the query packs that reward defensive code produce noise there
# rather than findings. They are still compiled and run by CI. `web/` is the
# renderer front-end and is analysed by its own bundler toolchain, not here.
paths:
  - bindings
  - crates
  - fuzz
