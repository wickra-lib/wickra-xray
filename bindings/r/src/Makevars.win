# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_xray.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKXRAY_INC)
PKG_LIBS = -L$(WKXRAY_LIB) -l:wickra_xray.dll
